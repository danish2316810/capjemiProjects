_schema-version: "3.2"
ID: appforgit
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: appforgit-destination-content
  type: com.sap.application.content
  requires:
  - name: appforgit-destination-service
    parameters:
      content-target: true
  - name: appforgit-repo-host
    parameters:
      service-key:
        name: appforgit-repo-host-key
  - name: appforgit-uaa
    parameters:
      service-key:
        name: appforgit-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: appforgit_html_repo_host
          ServiceInstanceName: appforgit-html5-srv
          ServiceKeyName: appforgit-repo-host-key
          sap.cloud.service: appforgit
        - Authentication: OAuth2UserTokenExchange
          Name: appforgit_uaa
          ServiceInstanceName: appforgit-xsuaa-srv
          ServiceKeyName: appforgit-uaa-key
          sap.cloud.service: appforgit
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: appforgit-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: appforgit-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - appforgit.zip
      name: appforgit
      target-path: resources/
- name: appforgit
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: appforgit-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: appforgit-destination-service
    service-plan: lite
- name: appforgit-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: appforgit-xsuaa-srv
    service-plan: application
- name: appforgit-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: appforgit-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
